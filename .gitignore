# ============================================================
# 项目根 .gitignore
# 项目类型：Web (React + Vite，由 Atoms 平台生成)
# 历史：原 Unity 工程（已删除）的 .gitignore 已重写为 Web 项目版本
#
# 子项目级 .gitignore 处理子项目自有产物：
#   atoms/Dungeon‌sHR/.gitignore — Atoms 工程的 dist / node_modules / 构建缓存
# ============================================================

# ---- OS 系统文件 ----
.DS_Store
Thumbs.db
desktop.ini
__MACOSX/
*~

# ---- 编辑器 / IDE ----
.idea/
*.iml
*.swp
*.swo
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ---- 日志 / 临时 / 备份 ----
*.log
*.tmp
*.bak
*.orig
*.dmp

# ---- 环境变量（防误传密钥）----
.env
.env.local
.env.*.local
!.env.example

# ---- tmp/ 工作目录：保留脚本 / 截图样本 / dry-run 产物，但忽略依赖与缓存 ----
# 注：tmp/node_modules/ 当前已有 102 个文件被历史 commit 追踪（误提交）
# 如需彻底从 git 下线：git rm -r --cached tmp/node_modules/ && git commit
/tmp/node_modules/
/tmp/.cache/
/tmp/dist/

# ============================================================
# 旧 Unity .gitignore 内容已移除（项目早已切换到 Atoms web 工程）
# 主要被移除的错误规则：
#   *.html  *.css  *.json  *.gen.cs   ← 这些会误伤 Atoms 工程文件
#   Library/  Temp/  Obj/  Build/      ← Unity 输出目录，本项目不存在
#   *.meta  *.unitypackage  *.apk      ← Unity 资产 / 打包格式
#   *.dll  *.exe  *.so  *.dylib        ← Unity 原生库
#   .config/dotnet-tools.json          ← .NET 工具链
# 若日后真的回到 Unity 开发，参考：
#   https://github.com/github/gitignore/blob/main/Unity.gitignore
# ============================================================
